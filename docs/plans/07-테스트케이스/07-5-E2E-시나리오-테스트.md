# 테스트케이스 명세서 - 5. E2E 시나리오 테스트

> **원본 문서**: [07-테스트케이스-명세서](../07-테스트케이스-명세서.md)

---

## 5. E2E 시나리오 테스트

### 5.1 정상 흐름

| TC ID | 테스트명 | 시나리오 | 기대 결과 | 우선순위 |
|-------|---------|---------|----------|---------|
| TC-E2E-FLOW-001 | 신규 파일 감지 -> 완전 동기화 | LGU+ Mock에 파일 추가 -> 폴링 대기 | 5초 내 감지 -> 다운로드 -> 업로드 -> 대시보드에 완료 표시 | P0 |
| TC-E2E-FLOW-002 | 전체 동기화 실행 | "전체 동기화" 버튼 클릭 | 진행률 표시 -> 전체 파일 동기화 완료 -> 요약 표시 | P0 |
| TC-E2E-FLOW-003 | 파일 탐색기에서 수동 동기화 | 파일 선택 -> "지금 동기화" 클릭 | 해당 파일 즉시 동기화, 상태 아이콘 변경 | P1 |
| TC-E2E-FLOW-004 | 설정 변경 -> 즉시 반영 | 폴링 간격 5초 -> 10초 변경 | 저장 후 즉시 10초 간격으로 폴링 | P1 |
| TC-E2E-FLOW-005 | 로그 뷰어 실시간 확인 | 동기화 진행 중 로그 탭 전환 | 실시간 로그 스트리밍 표시 | P1 |

### 5.2 실패 복구

| TC ID | 테스트명 | 시나리오 | 기대 결과 | 우선순위 |
|-------|---------|---------|----------|---------|
| TC-E2E-FLOW-006 | 다운로드 실패 -> 자동 재시도 -> 성공 | 1~2회차 Mock 실패, 3회차 성공 | 재시도 후 동기화 완료, 대시보드 정상 표시 | P0 |
| TC-E2E-FLOW-007 | DLQ 항목 수동 재시도 | 실패 파일 DLQ 이동 후 수동 재시도 | DLQ 목록에서 재시도 -> 성공 시 목록 제거 | P1 |

### 5.3 인증 만료

| TC ID | 테스트명 | 시나리오 | 기대 결과 | 우선순위 |
|-------|---------|---------|----------|---------|
| TC-E2E-FLOW-008 | 세션 만료 -> 자동 재인증 -> 동기화 재개 | 폴링 중 Mock이 401 반환 | 자동 재로그인 -> 폴링 재개 -> 대시보드 정상 | P0 |
| TC-E2E-FLOW-009 | 재인증 실패 -> 사용자 알림 | 자격증명 변경 시뮬레이션 | 토스트 알림 + 설정 화면 유도 | P1 |

### 5.4 대량 파일

| TC ID | 테스트명 | 시나리오 | 기대 결과 | 우선순위 |
|-------|---------|---------|----------|---------|
| TC-E2E-FLOW-010 | 100개 파일 동시 감지 | Mock에 100개 파일 히스토리 추가 | 순차 처리 (동시 5개), 전체 완료, 통계 100건 | P1 |
| TC-E2E-FLOW-011 | 대량 처리 중 UI 응답성 | 100개 처리 중 대시보드 조작 | UI 100ms 이내 응답, 프로그레스 실시간 갱신 | P1 |

### 5.5 프로그램 재시작

| TC ID | 테스트명 | 시나리오 | 기대 결과 | 우선순위 |
|-------|---------|---------|----------|---------|
| TC-E2E-FLOW-012 | 체크포인트 복구 후 이어받기 | 50건 처리 후 앱 종료 -> 재시작 | 51건째부터 이어서 처리, 중복 없음 | P0 |
| TC-E2E-FLOW-013 | 비정상 종료 후 임시 파일 정리 | 다운로드 중 프로세스 Kill -> 재시작 | 불완전 .tmp 파일 삭제, 해당 파일 재다운로드 | P1 |

### 5.6 설정 변경

| TC ID | 테스트명 | 시나리오 | 기대 결과 | 우선순위 |
|-------|---------|---------|----------|---------|
| TC-E2E-FLOW-014 | 동기화 폴더 추가 | 새 폴더 체크 활성화 | 해당 폴더 파일 감지 시작, 기존 파일 전체 동기화 | P1 |
| TC-E2E-FLOW-015 | 동기화 폴더 제거 | 기존 폴더 체크 해제 | 해당 폴더 감지 중단 (기존 파일은 보존) | P1 |
